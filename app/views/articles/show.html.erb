<h1><%= @article.title %></h1>

<p><%= @article.text %></p>


<h1>Комментарии: </h1>

<%= 'Без коментариев' if @article.comments.empty? %>

<% @article.comments.each do |comment|%>

<p><strong>Name: </strong><%= current_user.username %></p>
<%= comment.body %></p>
<hr>

<% end %>

<p>
<%= form_for([@article, @article.comments.build]) do |f| %>

  
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>

  <p><%= f.submit %></p>

<% end %>
</p>